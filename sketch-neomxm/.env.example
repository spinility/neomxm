# Example .env file for sketch-neomxm
# Copy this to .env and fill in your actual API keys
# IMPORTANT: Add .env to your .gitignore to avoid committing secrets!

# === API Keys ===
# At least one API key is required for LLM functionality

# Anthropic Claude (recommended for sketch-neomxm)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI (alternative)
#OPENAI_API_KEY=sk-your-openai-key-here

# DeepSeek (alternative, cheap option)
#DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# Google Gemini (alternative)
#GEMINI_API_KEY=your-gemini-key-here

# === Cortex Configuration ===
# Cortex routes requests to appropriate models for cost optimization

# URL of the Cortex server (required for Cortex routing)
CORTEX_URL=http://localhost:8181

# Enable Cortex routing (optional, default: true if CORTEX_URL is set)
#CORTEX_ENABLED=true

# Cortex profiles directory (optional)
#CORTEX_PROFILES_DIR=cortex/profiles

# Cortex logs directory (optional)
#CORTEX_LOGS_DIR=cortex/logs

# Enable debug logging for Cortex (optional)
#CORTEX_DEBUG=true

# === Model Overrides (Optional) ===
# Override which models Cortex uses for different task levels

# Simple tasks (default: gpt-4o-mini)
#CORTEX_MODEL_FIRSTATTENDANT=gpt-4o-mini

# Complex tasks (default: claude-sonnet-4.5)
#CORTEX_MODEL_SECONDTHOUGHT=claude-sonnet-4.5

# Elite/challenging tasks (default: claude-opus-4)
#CORTEX_MODEL_ELITE=claude-opus-4

# === Other Configuration ===
# Additional environment variables can be added here
